# RPi5 CPU-only requirements
# Install PyTorch CPU build first:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

# Core ML
transformers>=4.57.0
accelerate>=0.30.0
tokenizers>=0.19.0

# CPU quantization (replaces bitsandbytes + autoawq)
optimum-quanto>=0.2.4

# Evaluation & datasets
datasets>=2.19.0
evaluate>=0.4.1
nltk>=3.8.1

# Vision
Pillow>=10.3.0

# Profiling (CPU/RAM)
psutil>=5.9.8

# Data & results
pandas>=2.2.0
numpy>=1.26.0
tqdm>=4.66.0
pyyaml>=6.0.1
